import React from 'react';
import './LoadingSpinner.css';

// Loading message used while fetching API data

const LoadingSpinner = () => {
  return <div className="LoadingSpinner">Loading ...</div>;
};

export default LoadingSpinner;
